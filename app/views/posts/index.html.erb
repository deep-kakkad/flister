<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
<style>
     td{padding:6px;}
     th{padding:6px;}

 .sbl
   {
    display:block;
    border:1px solid #999999;
    border-radius:5px;  
   }
  a:hover
  {
    padding:8px 8px 8px 8px;
    border:solid;
    background-color:gray;
    border-right-color:gray;
    border-bottom-color:gray;
    border-left-color:black;
    border-top-color:black;
  }
</style>
</head>
<body>

<center>
<fieldset class="sbl">
<p id="notice"><%= notice %></p>

<h1>Listing Posts</h1>
<% if @posts.present? %>
<table>
  <thead>
    <tr>
      <th>Image</th>
      <th>Content</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  <%# abort @posts.collect(&:user_id).inspect %>
    <% @posts.each do |post| %>
      <tr>
        <td align="center"><%= image_tag post.image.url(:thumb) %></td>
        <td align="center"><%= post.content %></td>
        <td align="center"><%= post.user.email %></td>
        <td align="center"><%= link_to 'Show', post %></td>
        <td align="center"><%= link_to 'Edit', edit_post_path(post) %></td>
        <td align="center"><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<% else %>
  User and him/her friends have no post....
 <% end %> 
<br>

<a><%= link_to 'New Post', new_post_path %></a>
<a><%= link_to 'Back', root_path %></a>
</fieldset>
</center>
</body>
</html>
